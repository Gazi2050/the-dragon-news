import { Box, Container } from '@mui/material';
import React from 'react';

const Header = () => {
    return (
        <Box>
            <Container>

            </Container>
        </Box>
    );
};

export default Header;